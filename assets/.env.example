# Flutter app config — copy to `assets/.env` and adjust for your setup.
# Android emulator: use http://10.0.2.2:3000 instead of localhost for BASE_URL.

BASE_URL=http://localhost:3000
GOOGLE_MAPS_API_KEY=your_google_maps_api_key
